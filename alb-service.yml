# service type loadbalancer       
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: java-app
  name: java-app
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 8080
  type: LoadBalancer
  selector:
    app: java-app